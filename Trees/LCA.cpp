#include <bits/stdc++.h>
using namespace std;

class TreeNode {
    public : 
    int val;
    TreeNode*left;
    TreeNode*right;

    TreeNode (int x){
        val = x;
        left = NULL;
        right = NULL;
    }
};
class Solution {
  TreeNode*LCA(TreeNode *p, TreeNode *q, TreeNode *q){
    if (root == nullptr || root == q || root = p){
        return root;
    }
    TreeNode* LCA = LCA()
  }
};